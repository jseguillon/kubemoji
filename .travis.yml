sudo: required
services: docker
language:  minimal
dist: xenial
# env:
#   - VAR=val


#thanks to https://github.com/travis-ci/travis-ci/issues/5358#issuecomment-248915326

cache:
  directories:
    - $HOME/todo

#before_install:
#  - nvm install node npm
#  - nvm use node

script:
  - sudo apt-get install asciinema
  - asciinema -y -c "/bin/sh -c 'cat test.txt; echo 👂     📲      📡      🎙       🎶      🎼      📠      🛰️'" rec
